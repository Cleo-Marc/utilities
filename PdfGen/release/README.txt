To register the PdfGen COM object:

1. Start a DOS command prompt running as Administrator
2. Cd to the directory containing the PdgGen DLLs (and this README)
3. Run the register.bat script
4. Ignore annoying RegAsm warning
